to convert xlsx to csv use this command in the main dir:
-- python3 scripts/xlsx_to_csv.py data/raw/sample.xlsx -o data/processed/output.csv

converted and joined all xlsx to a single csv - done

Data preprocessing
- Convert categorical features (Përshkrimi i Sektorit, Komuna, Statusi i Regjistrimit, Përshkrimi i Gjobave) into numerical format (one-hot encoding or label encoding).
- Handle missing values.
- Normalize or scale numerical features if needed.

- choose model for training. Best suited:
    - Linear Regression for a simple baseline
    - Random Forest Regressor and XGBoost for better accuracy.

------
totali 28171 
Pershkrimi i sektorit -> 8 kategori (kate1, kate2, ..., kate8), numri total per cdo kategori. Shumen per cdo kategori
Komuna (37), numri total krahasohet me 28171
statusi i regjistrimit (psh 4 kategori), numri total krahasohet me 28171
pershkrimi i gjobave (kategorizu), numri total krahasohet me 28171

per krahasim te shumes se totalit (bone ni funksion qe e krahason me 28171)

func ():
sum(array) === 28171

per konvertim ne numer (?a duhet te fillohet prej 0 apo prej 1?)
------